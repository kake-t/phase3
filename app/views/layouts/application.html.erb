<!DOCTYPE html>
<html>
  <head>
    <title>Phase3</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
<!-- header -->
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <!-- ナビバーのヘッダー -->
        <div class="navbar-header">
          <a class="navbar-brand" href="/">Clone</a>
        </div>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to '一覧', pictures_path %></li>
            <!-- ログイン中か否かで表示が変わる -->
            <% if logged_in? %>
              <li><%= link_to "新規投稿", new_picture_path %></li>
              <li><%= link_to "#{current_user.name}のページ", user_path(current_user.id) %></li>
              <li><%= link_to "ログアウト", session_path(current_user.id), method: :delete %></li>
            <% else %>
              <li><%= link_to "ユーザー登録", new_user_path %></li>
              <li><%= link_to "ログイン", new_session_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <%= yield %>

<!-- footer -->
    <footer class="footer">
      <div class="container">
        <p class="text-muted text-center">Instagram clone 2018</p>
      </div>
    </footer>
  </body>
</html>
