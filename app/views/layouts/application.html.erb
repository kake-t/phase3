<!DOCTYPE html>
<html>
  <head>
    <title>Phase3</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <%= link_to 'top', '/' %>
      <!-- ログイン中か否かで表示が変わる -->
      <% if logged_in? %>
        <%= link_to "#{current_user.name}のページ", user_path(current_user.id) %>
        <%= link_to "session_destroy", session_path(current_user.id), method: :delete %>
      <% else %>
        <%= link_to "user_new", new_user_path %>
        <%= link_to "session_new", new_session_path %>
      <% end %>
    </header>

    <%= yield %>

    <footer class="footer">
      <div class="container">
        <p class="text-muted text-center">Instagram clone 2018</p>
      </div>
    </footer>
  </body>
</html>
